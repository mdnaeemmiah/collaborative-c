import LoginForm from '@/components/moduls/auth/Loginform';
import React from 'react';

const page = () => {
    return (
        <div>
           <LoginForm></LoginForm>
        </div>
    );
};

export default page;