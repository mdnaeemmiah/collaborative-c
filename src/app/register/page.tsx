import RegisterForm from '@/components/moduls/auth/RegisterForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <RegisterForm></RegisterForm>
        </div>
    );
};

export default page;